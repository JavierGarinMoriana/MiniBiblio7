{% extends 'layout.html.twig' %}

{% block body %}

    {% block content %}
        <h1>Eliminar Libro</h1>
        <div class="alert alert-danger">
            El libro {{ libro.titulo }} será eliminado de la aplicación.
            La operación no se puede deshacer.
        </div>
        <form method="post">
            <a href="{{ path('listar') }}" class="btn btn-info">Volver al listado</a>
            <button type="submit" name="confirmar" class="btn btn-danger">Confirmar eliminación</button>
        </form>
    {% endblock %}

{% endblock %}


